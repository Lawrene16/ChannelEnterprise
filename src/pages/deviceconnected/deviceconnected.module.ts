import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DeviceconnectedPage } from './deviceconnected';

@NgModule({
  declarations: [
    DeviceconnectedPage,
  ],
  imports: [
    IonicPageModule.forChild(DeviceconnectedPage),
  ],
})
export class DeviceconnectedPageModule {}
